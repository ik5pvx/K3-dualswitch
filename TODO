
* Work on conflict logic (mostly done, need more testing)
* Add manual/auto operation (done)
* Buttons to cycle through available antennas (done)
* Actually read real band data lines
* Save antenna mapping to eeprom instead of having to recompile
* LCD ?
* Use the relay module instead of the leds (one module tested).
* Assert TX-Inhibit while switching or if bad antenna selected
* Check power budget.

